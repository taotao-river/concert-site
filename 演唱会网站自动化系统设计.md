# 演唱会信息网站 · 自动化系统设计

> 目标：网站内容更新自动化 + 社媒内容同步自动化，尽量少人工干预。  
> 核心结论先行：**网站可高度自动化，社媒建议半自动化（自动生成 + 定时发布 + 人工抽检），纯全自动有封号和内容事故风险。**

---

## 1. 自动化目标与边界

| 环节 | 能否全自动 | 说明 |
|---|---|---|
| 数据采集（爬官方） | 高度可自动化 | 定时爬虫即可，但反爬/页面改版需维护 |
| 数据清洗去重 | 高度可自动化 | 写规则 + AI 辅助 |
| 网站发布更新 | 高度可自动化 | 采集到新数据自动入库上线 |
| 社媒文案生成 | 可自动化 | 用 AI 把结构化数据转成各平台文案 |
| 社媒发布 | 部分平台可自动化 | 见第 5 节平台能力表 |
| 内容审核/纠错 | 不能全自动 | 必须保留人工抽检兜底 |

---

## 2. 整体流水线架构

```
定时任务（调度器，按时间自动触发）
        ↓
【采集层】爬虫抓官方票务/艺人官网的新信息
        ↓
【处理层】清洗、去重、标准化、识别「新增/变更/取消」
        ↓
【生成层】AI 把结构化数据生成：
          - 网站内容
          - 各社媒平台的标题/文案/配图文案
        ↓
【发布层】
          - 网站：自动写入数据库、上线展示
          - 社媒：自动发布 或 存为待确认草稿
        ↓
【监控层】失败重试、异常告警、人工兜底
```

---

## 3. 各模块技术方案

### 3.1 调度器（定时触发）

推荐：**n8n（免费、可视化、新手友好）** 或 GitHub Actions（免费定时）。  
用它们按固定时间（如每小时/每 6 小时）触发采集和发布流程。

### 3.2 采集层

- Python 爬虫脚本，抓取官方票务平台、艺人官网的公开事实信息
- 注意：遵守 robots、低频、只抓事实字段
- 用 Playwright 处理需要渲染的页面

### 3.3 处理层

- 去重：同一场次不同来源合并
- 标准化：城市名、艺人名、币种、时区统一
- 状态识别：新增 / 变更（时间票价变了）/ 取消

### 3.4 生成层（AI 的关键作用）

让 AI 把结构化数据「翻译」成不同平台的内容：

- 网站：标准详情字段 + 简介
- 微博：带话题的短文案 + 开票提醒
- 公众号：攻略式长文摘要
- X：英文短帖（面向海外）
- 小红书：种草语气 + 标签

这一步是「一次数据、多渠道改编」，也是纯自动化的核心价值。

### 3.5 发布层

- 网站：直接写数据库，自动上线
- 社媒：有 API 的平台自动发；无 API 的平台自动生成草稿，你确认后发

### 3.6 监控层

- 采集失败、发布失败自动重试
- 异常推送到你的邮箱/微信，人工介入
- 关键字段缺失时阻断发布，避免发错信息

---

## 4. 网站内容自动化（重点，可全自动）

这部分最靠谱，建议先做全自动：

```
定时爬虫 → 清洗 → 对比已有数据 → 有新增/变更 → 自动入库 → 网站展示
```

唯一需要人做的是：定期看监控报告，确认有没有漏抓、错抓。

---

## 5. 社媒平台自动化能力对照表（关键）

不是所有平台都能自动发，这是你「纯自动化」想法最大的现实约束：

| 平台 | 能否自动发布 | 说明 |
|---|---|---|
| X / Twitter | 可以 | API v2 支持发帖，需开发者账号，免费档有限额 |
| 微博 | 可以 | 开放平台 API，需申请应用审核，有频率限制 |
| 微信公众号 | 部分 | 个人订阅号无群发 API；认证服务号才有，且有次数限制 |
| Telegram 频道 | 可以（最省心） | Bot API 全自动，但国内用户少 |
| 小红书 | 基本不可 | 无公开 API，只能 RPA 或手动 |
| B站 | 部分 | 开放平台可投稿，但审核严格、个人受限 |
| 抖音 | 部分 | 需企业认证，个人基本不可 |
| Instagram | 可以 | Meta Graph API，需专业/创作者账号 |

**结论：**

- X、微博、Telegram、Instagram 可以真自动
- 公众号（个人订阅号）、小红书、抖音基本不能全自动
- 所以「全平台自动同步」不现实，要接受部分平台手动或半自动

---

## 6. 现实建议：网站全自动 + 社媒半自动

推荐落地形态：

1. 网站内容更新：**全自动**（采集 → 清洗 → 入库 → 上线）
2. 社媒文案：**AI 全自动生成**
3. 社媒发布：有 API 的平台**定时自动发**；无 API 的平台**自动生成草稿，你每天花 10 分钟确认**
4. 每周抽检：人工抽查自动发布的内容有没有错

这样既省人力，又避免「机器人账号被限流封号」和「自动发错信息」两个大坑。

---

## 7. 风险与防护

| 风险 | 防护 |
|---|---|
| 社媒账号被判定营销号/机器人封号 | 保留人工确认环节、发布频率克制、文案人格化 |
| 自动发布错误/过时信息 | 关键字段校验、异常阻断、人工抽检 |
| 爬虫触发反爬被封 IP | 低频抓取、遵守 robots、轮换代理 |
| 平台改版导致采集失效 | 监控告警、定期维护脚本 |
| 版权/合规问题 | 只发事实 + 官方跳转，不搬图片文案 |

---

## 8. 技术选型（配合 AI 自建）

| 模块 | 推荐方案 | 说明 |
|---|---|---|
| 调度 | n8n（自托管免费）或 GitHub Actions | 新手友好 |
| 采集 | Python + Playwright | AI 帮你写 |
| 数据库 | Supabase | 免费额度够用 |
| AI 生成 | 调用大模型 API（如 GPT/Claude） | 按量付费，量小很便宜 |
| 社媒发布 | 各平台官方 API / 第三方 SDK | 有 API 的平台 |
| 网站部署 | Vercel | 一键部署 |
| 监控告警 | n8n 内置 / Sentry | 失败自动通知 |

---

## 9. 分步实施顺序

1. 先做**网站采集 + 自动入库**（最核心、最可靠）
2. 再做 **AI 生成社媒文案 + 存草稿**（不直接发）
3. 开通 X / 微博 API，做**定时自动发布**
4. 最后对无 API 的平台做「草稿 + 人工确认」
5. 全程加监控告警

---

## 10. 一句话总结

**「全自动」理想很好，但落地应该是：采集和网站更新全自动，社媒发布半自动 + 人工兜底。** 这样既最大化省人力，又不至于因为一条自动发错的内容或一个被封的账号让整个项目崩掉。

> 记于 2026-09-07。配合《演唱会信息网站产品架构文档.md》《演唱会网站AI自建指南.md》《演唱会网站商业模式分析.md》使用。
