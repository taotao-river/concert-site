# CLAUDE.md · 项目工作规范

> 本文件是这个仓库的**约束层**。任何人（含 AI）在动手前先读这里。
> 与其他任何文档冲突时，**以本文件的规则为准**（注意：规则以本文件为准，事实与结论以第 2 节的权威顺序为准）。

---

## 1. 项目是什么 / 现在在哪一阶段

**一句话**：面向中国（华人）粉丝的欧美明星巡演信息与抢票服务。不卖票、不代订、不碰黄牛。

**当前阶段：第 0 阶段（需求验证期）。**

| 阶段 | 判据 | 允许做的事 |
|---|---|---|
| **0 验证期（现在）** | 未通过 H1/H2/H3（见第 6 节） | 只做内容与人肉服务。**不写产品代码、不加表、不做用户体系** |
| 1 标准化期 | H1+H2 通过 | 把人肉服务做成标准化交付物；极简静态站 |
| 2 产品期 | H2 稳定复现 3 个月 | 才允许做开票提醒系统、订阅、用户体系 |

**第 0 阶段的精力分配是硬约束：内容 80% / 私域 15% / 网站 5%。**
写代码之前先问一句：这行代码能让 H2（有人付钱）更快出结果吗？不能就别写。

> 已有的 `database/`、`scripts/`、`concert-map-prototype.html` 是第 0 阶段之前的产物，属于**已投入的沉没资产**：可以维护、可以用作校验工具，但不要在验证通过前继续往里加功能。

---

## 2. 文档权威顺序（冲突时看这里）

仓库里的方案文档**不是平级的**。按以下三层判断谁说了算（`README.md` 把文档导航委托给了本节，所以这里的条目都是可点的）：

### 第一层 · 权威（结论以它们为准）

| 文档 | 管什么 |
|---|---|
| [项目总框架_MasterPlan.md](项目总框架_MasterPlan.md) | 战略、定位、产品结构、路线图 |
| [商业化可行性分析_独立复核.md](商业化可行性分析_独立复核.md) | **一切财务数字、收入模型、变现优先级、验证判据** |
| [香港身份运营方案_法律与落地分析.md](香港身份运营方案_法律与落地分析.md) | 法律边界、主体选择、收款架构 |

三者之间若冲突：财务看复核，法律看香港方案，其余看 MasterPlan。

### 第二层 · 参考（可用，但不构成决策依据）

| 文档 | 说明 |
|---|---|
| [演唱会信息网站产品架构文档.md](演唱会信息网站产品架构文档.md) | 技术架构细节可用；**其中的定位、分类频道、路线图已过时** |
| [演唱会网站自动化系统设计.md](演唱会网站自动化系统设计.md) | 流水线设计可用；但属第 2 阶段内容，现在别做 |

### 第三层 · 历史存档（只读，不要引用其结论）

| 文档 | 为什么废止 |
|---|---|
| [演唱会网站商业模式分析.md](演唱会网站商业模式分析.md) | KPOP/内娱口径，战略已转向欧美，**整体废止** |
| [演唱会网站AI自建指南.md](演唱会网站AI自建指南.md) | 工具链（第 2 节）仍可用；圈层选择、执行顺序已废止 |
| [欧美明星演唱会信息站_方案与商业分析.md](欧美明星演唱会信息站_方案与商业分析.md) | 第 1 节战略论证、第 3 节成本成立；**第 4、5 节收入测算已被证伪** |
| [欧美明星抢票攻略站_可行性与项目方案.md](欧美明星抢票攻略站_可行性与项目方案.md) | 第 1、2、5 节成立；**4.3 节是全 hub 唯一算对的联盟测算**；第 6、7 节订阅优先级与 SOP 已被推翻 |

**引用规则：写新内容时不要引用第三层的数字。第三层文档顶部必须带存档横幅。**

---

## 3. 财务口径（唯一允许的算式）

历史上这个 hub 出过同一件事差 55-100 倍的两套算法。为杜绝再发生，**所有收入测算必须使用下面的算式，不得简写、不得跳步**：

```
联盟收入 = 攻略页PV × 联盟点击率(1-5%) × 下单转化率(1-2%) × 平均佣金(150-350元)
                                          ↑
                    这一层永远不许省略。点击 ≠ 下单。

订阅/付费收入 = 可触达人数 × 付费率 × 客单价
                              ↑
              一次性攻略包 1-3%；年订阅 0.02-0.15%。不得混用。
```

**三条硬性要求：**

1. 任何收入数字必须写明**假设的每一层取值**，不能只给结论；
2. 转化率不得超过本节给出的区间上限，超了要先在文档里论证为什么；
3. 「累计付费人数 ÷ 单月访客数」不是转化率，禁止这样算。

**当前基准（首年，来自独立复核第 2.3 节）**：收入中位数 1.5-3 万元，成本 1.2-3.6 万元，现金流转正在第 12-18 月。任何新方案若给出显著更高的数字，必须先解释它为什么能跑赢这个基准。

---

## 4. 事实与数据纪律

这个项目的立身之本是信息准确。信息错一次，付费用户就没了。

- **不编造巡演事实。** 艺人是否在巡演、场次日期、场馆、票价，只能来自官方公告、官方票务平台或 API 返回。凭印象写的一律标 `待核实`。
- **每条演出信息必须带来源和核实时间**（`events.last_verified_at`、`event_sources`）。没有来源的数据不入库。
- **`待核实` 是合法状态，猜测不是。** 宁可留空，不要填一个看起来合理的值。
- **数据源在使用前必须确认商业授权**，不只是技术可用性。见第 5 节。
- 用户纠错入口是必需品，不是加分项。

---

## 5. 数据源使用规则

**在写任何依赖某个数据源的代码之前，先确认它的 TOS 允许你的商业形态（带联盟链接/付费墙的站点）。**

| 数据源 | 状态 | 规则 |
|---|---|---|
| Ticketmaster Discovery API | 可用，商用待确认 | 免费档 5,000 次/日、5 次/秒。上线带联盟链接前须确认授权 |
| Spotify Web API | 部分可用 | 仅用 `search` 取 popularity。`related-artists`、`recommendations`、`audio-features` 已对新应用停用，**不要再写调用** |
| Songkick API | **不可作为主数据源** | 新 key 基本不再发放，需实测后再决定 |
| Bandsintown API | 待确认 | 商业用途需谈合作 |
| 艺人官网 / 官方社媒 | **首年主数据源** | 加场、预售码这类高价值信息只有这里有 |
| 人工录入 + 用户投稿 | **首年主数据源** | 10-20 个艺人的量级，人工完全跑得动且更准 |

**判断原则：数据不是壁垒，数据获取权是风险。** 首年靠人工，API 只做校验。

---

## 6. 验证门（go / no-go）

第 0 阶段只有一个任务：跑完这三条。**未通过前不进入下一阶段。**

| 假设 | 动作 | 通过判据 | 不通过 |
|---|---|---|---|
| H1 需求存在 | 小红书发 10 篇巡演场次+抢票时间线 | 单篇收藏 > 500 或累计私信 > 20 | 换艺人再跑一轮；两轮不过则停 |
| **H2 有支付意愿** | 对私信用户报价 99 元 1v1 咨询 | **4 周内成交 ≥ 5 单** | 转纯流量模型，放弃付费 |
| H3 联盟能转化 | 给咨询用户发 Agoda/Trip.com 链接 | ≥ 1 笔真实佣金入账 | 联盟从收入模型中划掉 |

**H2 是唯一真正重要的那条。它不过，后面所有模型都不用算。**

---

## 7. 艺人选择规则

首批艺人**按商业价值排，不按名气排**。名气榜是错的：不在巡演的顶流产生不了任何交易。

```
优先级 = 是否在巡演(0/1) × 亚洲可达场次数 × 中文搜索热度 ÷ 已有中文攻略供给
```

- `是否在巡演` 是**否决项**：为 0 则整体为 0，不进首批名单；
- `亚洲可达`指新加坡/东京/首尔/曼谷/香港/吉隆坡/雅加达/台北等大陆粉丝短途可达城市；
- 已有攻略供给多的顶流，竞争激烈、边际价值低；中腰部艺人反而是空白。

无巡演的艺人保留在 `artists` 表但置 `is_active = false`，不出现在首页与内容排期。

---

## 8. 合规红线（业务侧）

以下任何一条越界，项目性质就变了：

- ❌ 售票、代购、代抢、代订、代收代付任何票款或行程款
- ❌ 组团收费、打包行程收费（1v1 只做**信息咨询**，用户自己在平台下单）
- ❌ 教用户使用脚本/机器人/绕验证码/代理 IP 绕过地区限制
- ❌ 传播共享预售码等违反票务平台用户协议的操作（会导致用户被砍单）
- ❌ 存储艺人图片、搬运官方宣传文案

必做：

- ✅ 全站明示「不售票、不代订，仅提供信息与攻略」
- ✅ 联盟链接标注「含推广链接」
- ✅ 每条信息标来源 + 最后更新时间
- ✅ 收款、行程预订、履约三者主体分离

---

## 9. 目录与命名约定

```
/                       方案文档（中文名，下划线分隔子标题）
  CLAUDE.md             本文件，约束层
  README.md             导航，只放索引和现状，不放结论
  database/             SQL，仅第 1 阶段之后使用
  scripts/              数据校验工具，非主数据链路
  content/              ← 第 0 阶段的主战场（内容稿件，按 艺人-主题.md 命名）
```

- 文档一律 Markdown，中文命名，不加日期后缀（版本靠 git）；
- **不新增文档，优先改现有文档。**这个 hub 已经因为文档过多而产生过冲突；
- 确需新增时，必须同时在 README 索引和本文件第 2 节的分层表里登记；
- 废止一份文档 = 在顶部加存档横幅 + 在第 2 节移到第三层，**不删文件**。

---

## 10. 操作红线（AI 必须先问）

即使在 auto-accept 模式下，以下操作必须停下来问：

- 删除文件、目录或 git 历史
- `git push` / `rebase` / `reset --hard` / 强制推送
- 修改 `.env`、密钥、token、CI 配置
- 数据库 schema 变更或数据迁移
- 公开发布（域名购买、网站上线、社媒账号注册、发文）

密钥、token 一律不进代码、不进 commit、不进日志。`scripts/` 里的 key 只从环境变量读。

---

## 11. 变更规则

**先改文档，再改实践，不要反过来。**

改动战略或财务假设时：

1. 先改第一层的对应文档；
2. 同步更新本文件里受影响的规则（尤其第 3、6、7 节的数字）；
3. 再改代码/内容；
4. commit message 说明改的是哪条假设、依据是什么。

---

## 12. 验证命令

改完之后自己跑一遍，不要只改不验：

```bash
# Python 脚本语法与导入检查
python3 -m py_compile scripts/fetch_tours.py

# 文档内部链接是否指向真实存在的文件
grep -oh '](\([^)]*\.md\))' *.md | sed 's/](\(.*\))/\1/' | sort -u | while read f; do
  [ -f "$f" ] || echo "断链: $f"
done

# 检查是否出现被废止的算式（点击直接当下单）
grep -n "点击转化\|× 300 元\|500 单" *.md
```

SQL 无法本地执行，改动 `database/*.sql` 后需在 Supabase SQL Editor 实跑一次确认。

> 记于 2026-09-08。本文件是约束层，改它需要显式决策，不要顺手改。
